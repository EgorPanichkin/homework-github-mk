// It's little conflict for your repo
function main() {
    let OS_b = 0;

    let name = "WIN32"
    let version = 11

    console.log(name + version);
    for (let x = 0; x >= 0; x++){
        console.log(OS_b);
    }
}

main()
